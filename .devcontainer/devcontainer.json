{
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {
  },
  "postCreateCommand": "pip install -r requirements.txt",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install ffmpeg libsm6 libxext6  -y"
  
}
